{
  "name": "groove",
  "private": true,
  "workspaces": [
    "backend/packages/*",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
